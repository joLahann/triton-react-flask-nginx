services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    image: wppipe-api
  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    image: wppipe-client
    ports:
      - "3000:80"
