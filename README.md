# React-Flask-Triton-Nginx Pipeline

docker compose build
docker compose up


curl "localhost:3000/api/resnet"
curl "localhost:3000/api/time"
curl "localhost:3000/api/journal_entries?mandant_id=John&year=Miami"

Browser ->
localhost:3000

